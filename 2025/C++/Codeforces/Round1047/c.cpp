#include <bits/stdc++.h>
#define int long long

using namespace std;

signed main() {
    int t; cin >> t;
    while (t--) {
        /*
        a*k + b/k = X , (X % 2 = 0)
        a*k^2 + b = X.k 

        -> a*k^2 & b is even
        -> a*k^2 & b is odd

        if (b is even):
            if (a is even):
                k is even
            if (a is odd):
                
        */
       if (b % 2 == 0) {

       }
    }
}