#include <bits/stdc++.h>
#define int long long
using namespace std;

struct point {
    int x, y;
}
signed main() {
    int n; cin >> n;
    vector<point> a(n + 1);
    for (int i = 1; i <= n; i++) {
        cin >> a[i].x >> a[i].y;
    }
    
}
