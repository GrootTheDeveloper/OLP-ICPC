#include <bits/stdc++.h>
using namespace std;
#define int long long
#define endl "\n"

signed main() {
    int n, c; cin >> n >> c;
    while (c--) {
        int type; char x, y;
        cin >> type >> x >> y;
    
    // [F, A, B]
    }
}