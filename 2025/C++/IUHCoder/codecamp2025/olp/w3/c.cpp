#include <bits/stdc++.h>
using namespace std;
#define int long long
#define endl "\n"
int n, m;

/*
Contest-Motto: Làm bài 1 cách dơ bẩn như 1 OLP-er thật thụ
*/

signed main() {
    cin >> n >> m;
    vector<int> t(n + 1), f(m + 1);
    for (int i = 1; i <= n; i++) {
        cin >> t[i];        
    }
    for (int j = 1; j <= m; j++) {
        cin >> f[j];
    }

    
}