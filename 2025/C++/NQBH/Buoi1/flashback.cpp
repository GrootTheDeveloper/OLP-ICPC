#include <bits/stdc++.h>
using namespace std;

const int MOD = 1e9 + 7;

int main(){
    freopen("file.inp", "r", stdin);
    freopen("file.out", "w", stdout);

    int t; cin >> t;
    while (t--) {
        int n, k; cin >> n >> k;
        vector<int> dp(n + 1, 0);
        dp[0] = n;
        for (int i = 0; i <= n; i++) {
            
        }
    }
}