#include <bits/stdc++.h>
#define int long long
const int oo = 1e18 + 7;
const int MOD = 1e9 + 9;
using namespace std;

signed main() {
    int t; cin >> t;
    while (t--) {
        int n; cin >> n;
        int ans = (16 * n * (n + 1)) % MOD * (2 * n + 1) % MOD;
    }
    return 0;
}