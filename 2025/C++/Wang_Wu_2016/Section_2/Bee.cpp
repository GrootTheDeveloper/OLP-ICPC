#include <bits/stdc++.h>
using namespace std;

const int magicBee = 1;

int main(){
/*
    int n; 
    while (cin >> n && n != -1) {
        int femaleBee = magicBee;
        int maleBee = 0;
        int year = 0;
        while(year < n) {
            int tempMaleBee = maleBee;
            int tempFemaleBee = femaleBee;

            femaleBee += tempMaleBee;
            maleBee += tempFemaleBee + tempMaleBee;


            maleBee -= tempMaleBee;
            year++;
        }        
        cout << maleBee << " " << maleBee + femaleBee << endl;
    }
*/
}