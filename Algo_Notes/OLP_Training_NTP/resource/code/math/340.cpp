#include <bits/stdc++.h>
#define int long long
using namespace std;
const int MAXN = 1e6;
vector<int> sang(MAXN + 5, 0);
signed main() {
    int q; cin >> q;
    while (q--) {
        int l, r; cin >> l >> r;
        
    }
}
